var chai = require("chai");
var chaiAsPromised = require("chai-as-promised");

var should = chai.should();
chai.use(chaiAsPromised);

describe("state generation", function () {
  it("should generate state data for a template");
});
